# deployment-pipeline
This project demonstrates a CI/CD deployment pipeline using GitHub Actions. Every push to the main branch automatically runs lint checks and deploys the HTML/JavaScript project to GitHub Pages if all checks pass.
